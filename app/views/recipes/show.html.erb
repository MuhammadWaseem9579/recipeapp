<br><h1 class="center">Recipe Name : <%= @recipe.name.capitalize %></h1><br>
<div class="col-md-8 col-md-offset-4 center-v">
	<h4 class="center description"><strong>Description</strong></h4>
	<hr>
	<%= simple_format(@recipe.description) %>
	<p>
		Created by:
	</p>
	<p>
		<small><%= @recipe.chef.name %></small>
		<div class="pull-right">created: <%= time_ago_in_words(@recipe.created_at) %> ago</div>
	</p>
	<p>
		<%= link_to "Recipes List" , recipes_path , class: "btn btn-primary" %> 

		<% if logged_in? && (@recipe.chef == current_chef || current_chef.admin ) %>
			<%= link_to "Edit Recipe" , edit_recipe_path(@recipe) , class: "btn btn-warning" %> 
			<%= link_to "Delete Recipe", recipe_path(@recipe) , method: :delete , class: "btn btn-danger", data: {confirm: "Are you sure you wanna delete it?"}%>
		<% end %>
		
	</p>
</div>